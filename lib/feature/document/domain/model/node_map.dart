// import 'dart:collection';

// import 'package:dial_editor_flutter/feature/document/domain/model/node.dart';

class NodeMap {
  // NodeMap(this._nodes) {
  // _buildNodeMap();
  // }
  // final LinkedList<Node> _nodes;
  // final Map<String, Node> _nodeMap = {};

  // void _buildNodeMap() {
  //   _nodeMap.clear();
  //   for (final node in _nodes) {
  //     _nodeMap[node.id] = node;
  //   }
  // }

  // void addNode(Node node) {
  //   _nodes.add(node);
  //   _nodeMap[node.id] = node;
  // }

  // void removeNode(Node node) {
  //   _nodes.remove(node);
  //   _nodeMap.remove(node.id);
  // }

  // void updateNode(Node node) {
  //   _nodeMap[node.id] = node;
  // }

  // Node? getNodeById(String id) {
  //   return _nodeMap[id];
  // }
}
